const express = require("express");
const router = express.Router();


router.post("/getTheme", (req, res, next) => {
    let fetchedUser, theme;
    const email = req.body.email;

})

router.post('/setTheme', function (req, res) {
 
})

module.exports = router;